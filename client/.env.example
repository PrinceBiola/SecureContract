# Client Environment Variables

# API Base URL (production only - in dev, Vite proxy is used)
VITE_API_URL=http://localhost:3001

# WebSocket URL (optional - defaults to same as API)
VITE_WS_URL=ws://localhost:3001
